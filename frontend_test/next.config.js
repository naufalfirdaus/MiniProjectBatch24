/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost']
    },

    // daisyui: {
    //     themes: ["light", "dark", "emerald"],
    // },
    
    // plugins: [
    //     require('daisyui'),
    // ],
}

module.exports = nextConfig
